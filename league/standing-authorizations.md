# Standing authorizations

What the agent may do without a fresh yes, with the date granted and the limits. The default is nothing but lineup moves. Append, do not rewrite.

## YYYY-MM-DD: lineup moves

Standing authorization to set the lineup at any time. Report every move with the reasoning.

## (add as you go)

Examples the original owner added over the first week, for reference: time sensitive adds, drops and claims on a stated timer when unreachable (see `rules/timer-authorization.md`); one named bench spot usable without asking for any free agent who makes a difference; the IR slot managed automatically; FAAB bids up to a ceiling (Sleeper leagues). Trades are never covered.
