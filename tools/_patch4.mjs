import fs from 'node:fs';
const rep = (f, a, b) => { let s = fs.readFileSync(f, 'utf8'); if (!s.includes(a)) throw new Error(f + ' missing: ' + a.slice(0, 60)); fs.writeFileSync(f, s.replace(a, b)); };
rep('START-HERE.md', 'and which platform I use (ESPN or Sleeper; Yahoo is possible through its OAuth API, see tools/optional-integrations.md)', 'and which platform I use (ESPN, Sleeper or Yahoo)');
rep('START-HERE.md', 'Sleeper needs no cookies for reads.', 'Sleeper needs no cookies for reads. Yahoo needs a one time app registration and one pasted code: walk me through tools/yahoo-api-cheatsheet.md, run node tools/yahoo-auth.mjs, and read through tools/yahoo-snapshot-fetch.mjs; Yahoo has an official write API, so with the Read/Write scope you can make lineup moves and adds through it rather than the page.');
rep('START-HERE.md', 'espn-basketball-cheatsheet.md, espn-baseball-cheatsheet.md, nba-data-cheatsheet.md', 'espn-basketball-cheatsheet.md, espn-baseball-cheatsheet.md, yahoo-api-cheatsheet.md, nba-data-cheatsheet.md');
rep('README.md', '<img alt="Platforms" src="https://img.shields.io/badge/platforms-ESPN%20%7C%20Sleeper-22d3ee?style=for-the-badge">', '<img alt="Platforms" src="https://img.shields.io/badge/platforms-ESPN%20%7C%20Sleeper%20%7C%20Yahoo-22d3ee?style=for-the-badge">');
rep('README.md', 'Works with ESPN and Sleeper leagues.', 'Works with ESPN, Sleeper and Yahoo leagues.');
rep('README.md', '## Basketball and baseball', '## Yahoo\n\nYahoo has the only official fantasy API, with reads and writes for football, basketball, baseball and hockey behind OAuth. The kit ships the one time auth script (`tools/yahoo-auth.mjs`, you paste one code), a snapshot script that feeds the same hourly diff, and a cheat sheet with every endpoint the agent uses. Yahoo does not publish projections through the API, so the snapshot fills them from Sleeper\'s projections by name and says so when it cannot.\n\n## Basketball and baseball');
rep('README.md', '- The ESPN login lasts about a month, then you log in again. Sleeper reads need no login at all.', '- The ESPN login lasts about a month, then you log in again. Sleeper reads need no login at all. Yahoo needs a developer app and one pasted code, then refreshes itself.');
rep('KEYS.md', '## Where the keys go', '## Yahoo (only if your league is on Yahoo)\n\nNot a model key, but it lives in the same file. Create an app at https://developer.yahoo.com/apps/create/ (Installed Application, redirect `https://localhost:8080`, Fantasy Sports permission, Read/Write if you want the agent to set lineups through the API). Put the Client ID and Client Secret in `.env` as `YAHOO_CLIENT_ID` and `YAHOO_CLIENT_SECRET`, then run `node tools/yahoo-auth.mjs` and paste the code it asks for. It stores the refresh token and prints your league keys; put yours in `.env` as `YAHOO_LEAGUE_KEY`, plus `YAHOO_SPORT` (nfl, nba, mlb) and `YAHOO_SEASON`.\n\n## Where the keys go');
rep('tools/optional-integrations.md', '| yfpy, yahoo_fantasy_api (Yahoo) | optional | Yahoo leagues need OAuth; these Python libraries handle it for NFL, NBA, MLB and NHL. Reads only; writes still happen in the Yahoo app. The kit does not ship a Yahoo snapshot script yet; an agent with Python can build one from these in an hour. |', '| Yahoo Fantasy Sports API | **baked in** | Official API with reads and writes behind OAuth. `tools/yahoo-auth.mjs` does the one time auth, `tools/yahoo-snapshot-fetch.mjs` feeds the hourly diff, `tools/yahoo-api-cheatsheet.md` lists the calls. Projections are filled from Sleeper by name because Yahoo does not expose them. |\n| yfpy, yahoo_fantasy_api (Python) | optional | Wrap the same API with OAuth built in, for agents that run Python. |');
rep('rules/standing-league-watch.md', 'Each pass: run the snapshot fetch (`tools/snapshot-fetch.js` for ESPN, `tools/sleeper-snapshot-fetch.js` for Sleeper)', 'Each pass: run the snapshot fetch (`tools/snapshot-fetch.js` for ESPN, `tools/sleeper-snapshot-fetch.js` for Sleeper, `tools/yahoo-snapshot-fetch.mjs` for Yahoo)');
console.log('patched');
